# LoungeOS Staff Application Boundary

This directory is reserved for authenticated staff routes in the Expo
application. Staff routes will be protected by Firebase Authentication and
server-enforced permissions.

The existing staff preview remains intentionally non-operational until the
authenticated staff routes and role dashboards are introduced in their ordered
modules. Customer ordering never treats this preview as staff authorization.