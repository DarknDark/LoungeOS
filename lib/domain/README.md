# LoungeOS Domain Foundation

This package contains provider-neutral entities, settings, repository ports,
domain events, and Firestore collection names. It must not import React,
Express, Firebase, or a payment provider.

Infrastructure adapters and application services depend on these contracts.