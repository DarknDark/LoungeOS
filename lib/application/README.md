# LoungeOS Application Foundation

This package contains use-case and service contracts. Implementations belong in
application composition or infrastructure adapters; UI packages should depend
on these boundaries rather than on Firestore or provider SDKs.